# app/

Aquí vive el proyecto Expo (React Native + TypeScript).

Aún no inicializado. Para crearlo:

```bash
npx create-expo-app . --template blank-typescript
```

Ejecutado desde dentro de esta carpeta (`app/`), no desde la raíz del repo.
